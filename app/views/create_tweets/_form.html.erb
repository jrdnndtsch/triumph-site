<%= form_for(@create_tweet) do |f| %>
  <% if @create_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@create_tweet.errors.count, "error") %> prohibited this create_tweet from being saved:</h2>

      <ul>
      <% @create_tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
